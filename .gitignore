node_modules
dist
.DS_Store
.claude/

# Rate registry runtime config (Supabase URL + anon key). Never committed.
rate-registry/config.js
# Build-time generated static aggregate pages.
rate-registry/rates/
.env
